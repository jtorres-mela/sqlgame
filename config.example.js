// Copy this file to config.js and fill in your Supabase credentials.
// config.js is gitignored — never commit your real keys.
// Leave values empty to run the game offline with no cloud storage.
const CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
};
